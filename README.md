# emotion-multimodal
- Text → Emotion (Transformers)
- Image (face) → Emotion (FER)
- Audio (speech) → Emotion (Transformers audio-classification)

## Setup (Windows)

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
